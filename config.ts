export const dbConfig = {
    database: 'LearningFactDb',
    username: 'learningDbUser',
    password: 'root',
    host: 'localhost', // Your PostgreSQL server host
    //dialect: 'postgres', // Specify the PostgreSQL dialect (not necessary)
};